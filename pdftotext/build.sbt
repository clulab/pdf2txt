name := "pdf2txt-pdftotext"
description := "The pdf2txt-pdftotext subproject implements an interface to the tika PDF converters."

resolvers ++= Seq(
//  Resolvers.localResolver, // Reserve for Two Six.
//  Resolvers.clulabResolver // processors-models, transitive dependency
)

libraryDependencies ++= {
  Seq(
  )
}
