# There are no sbt-specific options, so use .jvmopts instead.
# If sbt exhausts memory, change the limit here.
# -J-Xmx3g
# -J-Dfile.encoding=UTF-8
# -J-XX:+CMSClassUnloadingEnabled
# -J-XX:+UseConcMarkSweepGC
