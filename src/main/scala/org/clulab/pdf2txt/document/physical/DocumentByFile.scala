package org.clulab.pdf2txt.document.physical

class DocumentByFile {
  // Paragraphs =
  // Sentences =
  // Words =
  // These can be lazy iterators
  // new DocumentByFile(file)
  // Get text of entire file here, even though a string
  // Get iterator through characters
  // Convert every character?
  // UnicodeProcessor, AccentProcessor, ParagraphProcessor
  // SoftHyphen Processor, SoftSpace Processor
}
